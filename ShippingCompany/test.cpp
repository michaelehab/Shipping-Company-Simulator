#include <iostream>
#include "PriorityQueue.h"
using namespace std;

int main() {
	//For testing PriorityQueue
	/*PriorityQueue<int> q;
	cout<< q.isEmpty()<<endl;
	q.push(5, 10);
	q.push(20, 2);
	q.push(25, 15);
	q.push(30, 6);
	q.push(40, 2);
	q.push(6, 50);
	q.PrintPriorityQueue();
	cout << q.isEmpty() << endl;
	int x;
	q.peek(x);
	cout <<"Front element is " << x << endl;
	q.pop(x);
	q.pop(x);
	q.pop(x);
	q.pop(x);
	q.pop(x);
	q.PrintPriorityQueue();
	q.pop(x);
	q.pop(x);
	q.PrintPriorityQueue();*/


	return 0;
}